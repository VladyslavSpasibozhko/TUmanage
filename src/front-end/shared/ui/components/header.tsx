// TODO:might it be better to move to layout lib
export default function Header() {
  return (
    <header className="h-14 border-b border-border bg-surface px-6 flex items-center shrink-0">
      <span className="text-sm font-semibold text-text-primary">Header</span>
    </header>
  );
}
